const DEFAULT_SETTINGS = {
  type: 'single',
  minValue: 0,
  maxValue: 100,
  step: 1,
  direction: 'horizontal',
  hint: true,
  scale: false,
  positionLength: null,
};

export default DEFAULT_SETTINGS;
