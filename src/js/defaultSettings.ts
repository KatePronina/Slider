const DEFAULT_SETTINGS = {
    type: 'range',
    minValue: 0,
    maxValue: 100,
    step: 1,
    direction: 'horizontal',
    hint: true,
    scale: false,
    configuration: false,
};

export default DEFAULT_SETTINGS;
