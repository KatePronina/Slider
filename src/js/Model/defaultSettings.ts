const DEFAULT_SETTINGS = {
    TYPE: 'range',
    MIN_VALUE: 0,
    MAX_VALUE: 100,
    STEP: 1,
    DIRECTION: 'horizontal',
    HINT: true,
    SCALE: false,
    CONFIGURE: false,
}

export default DEFAULT_SETTINGS;